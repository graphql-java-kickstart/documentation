#!/usr/bin/expect -f
set timeout -1
spawn scp -r public ${SFTP_USER}@graphql-java-kickstart.com.transurl.nl:/www
expect {
        password: {send "$SFTP_PASSWORD\r" ; exp_continue}
        eof exit
}
